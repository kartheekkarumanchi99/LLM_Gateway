import { PagePlaceholder } from '@/components/page-placeholder';

export default function Page() {
  return (
    <PagePlaceholder
      title="Benchmarks"
      description="Compare model performance across benchmarks."
    />
  );
}
